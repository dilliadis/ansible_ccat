---
- name: demo register
  hosts: ansible1
  tasks:
    - command: date
      register: thisdate
    - debug:
        var: thisdate
    - debug:
        msg: the date is {{ thisdate.stdout[:10] +  thisdate.stdout[-55555:] }}
