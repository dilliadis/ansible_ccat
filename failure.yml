---
- name: failure
  hosts: localhost
  tasks:
    - name: showing failure
      command: echo "hello world"
      ignore_errors: true
      register: command_result
        # failed_when: "'world' not in command_result.stdout"
    - debug:
        msg: "Hello world"
