---
- name: variables demo
  gather_facts: false
  vars:
    myuser: anna
  hosts: all
  tasks:
    - name: creating user {{ myuser }}
      user:
        name: "{{ myuser }}"
        shell: /bin/bash
        state: absent
