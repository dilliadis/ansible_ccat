---
- name: error demo
  hosts: ansible1
  ignore_errors: true
  gather_facts: false
  tasks:
    - name: copy a file
      copy:
        src: /tmp/hello
        dest: /tmp
    - name: showing syntax error
      debug:
        msg: this is a syntax error
    - name: running  this
      debug:
        msg: running this
